import _ from 'lodash';

module.exports = {
    
    objectExist (arr, obj) {

        // _.some(arr, function (fav) {
        //     // console.log("found it")
        //   return fav.properties.label.value === obj.properties.label.value;

        // });

        // return _.contains(arr,obj);

        // arr.forEach(function(fav) {
        //     if(fav.properties.label.value === obj.properties.label.value){
        //         return true;
        //     }else{
        //         return false
        //     }
        //   });

    }

};